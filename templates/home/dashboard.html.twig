{% extends 'base.html.twig' %}

{% block title %}Accueil
{% endblock %}

{% block right %}
	<div class="mt-3 px-3 p-2 bgo-80 card2 rounded">
		<h1>Tableau de bord</h1>
		<div class="jumbotron">
			<p class="text-light">Gérer vos projets</p>
		</div>
	</div>
	<div class="card  p-1">
		<table class="rounded table table-primary table-hover table-striped">
			<thead>
				<tr>
					<th class="thead rounded-l-top" scope="col">Nom</th>
					<th class="thead rounded-r-top" scope="col">Lien</th>
				</tr>
			</thead>
			<tbody>
				{% for folio in pagination %}
					<tr>
						<td>{{ folio.name }}</td>
						<td>{{ folio.link }}</td>
					</tr>
				{% endfor %}
			</tbody>
		</table>
		{{ knp_pagination_render(pagination) }}
	</div>
</div>{% endblock %}{% block extrajs %}
<script type="text/javascript" src="assets/js/common.js"></script>
<script type="text/javascript"></script>{% endblock %}
