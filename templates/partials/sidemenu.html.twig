<nav class="navbar navbar-expand-lg navbar-light">
    <a class="navbar-brand" href="#">Xenatronics</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarMenuPop"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="navbar-collapse" id="navbarMenuPop">
        <ul class="navbar-nav mr-auto">
            {% for item in menu %}
                <li class="nav-item ">
                    <a class="nav-link" href="{{ item.link }}"><span
                                class="{{ item.icon }}"></span>&nbsp;&nbsp;&nbsp; {{ item.name|capitalize }} </a>
                </li>
            {% endfor %}
        </ul>
    </div>
</nav>