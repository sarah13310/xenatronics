{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block right %}
    <div class="mt-3 card bg-opaque75 col-5">
        <p class="card-header info-color mb-3 font-weight-normal"><i class="bi bi-key l-left"></i>Inscription</p>

        {{ form_errors(registrationForm) }}
            {{ form_start(registrationForm) }}
            {{ form_row(registrationForm.email, {
                label:'Adresse mail :'
            }) }}

            {{ form_row(registrationForm.plainPassword, {
                label: 'Mot de passe :'
            }) }}
            <button type="submit" class="btn btn-lg btn-primary mt-2">Créer un compte</button>
        {{ form_end(registrationForm) }}

    </div>
{% endblock %}
